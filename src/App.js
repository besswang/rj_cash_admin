import React, { Component } from 'react';
import {
  Button
} from 'element-react';
import './App.css';

class App extends Component {
  render() {
    return (
      <Button type = "primary" > 登陆 </Button>
    );
  }
}

export default App;
